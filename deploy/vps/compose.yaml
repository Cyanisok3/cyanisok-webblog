services:
  web:
    build:
      context: ../..
      dockerfile: deploy/vps/Dockerfile
    image: cyan-field-notes:latest
    container_name: cyan-field-notes
    init: true
    ports:
      - "127.0.0.1:3001:3000"
    restart: unless-stopped
